import React from "react";
import "./styles/Loader.scss"

const Loader = () => {
    return (
        <div className="loading">
            <div></div>
        </div>
    );
};

export default Loader;